// import React from "react";
// import { Navigate } from "react-router-dom";

// // Define the ProtectedRoute component
// const ProtectedRoute = ({ children }) => {
//   // Fetch authentication token from localStorage or sessionStorage
// //   const accessToken = localStorage.getItem("authToken") || sessionStorage.getItem("authToken");

//   // If the user is not authenticated, redirect to the login page
// //   if (!accessToken) {
// //     return <Navigate to="/login" replace />;
// //   }

//   // If authenticated, render the children (protected components)
//   return children;
// };

// export default ProtectedRoute;
